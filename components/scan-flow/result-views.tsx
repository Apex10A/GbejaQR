"use client"

import {
  ShieldCheck,
  ShieldAlert,
  ShieldX,
  ArrowLeft,
  ArrowRight,
  Link2,
  Info,
  Shield,
  AlertTriangle,
} from "lucide-react"
import { Button } from "@/components/ui/button"

export type ResultType = "verified" | "suspicious" | "malicious" | "unsafe"

interface ResultProps {
  onClose: () => void
}

const MOCK_URL = "https://secure-portal.verified-pu..."

// ===== VERIFIED =====
export function VerifiedResult({ onClose }: ResultProps) {
  return (
    <div className="flex min-h-screen items-center justify-center px-4 py-8">
      <div className="w-full max-w-md">
        <div className="overflow-hidden rounded-2xl border border-border bg-card">
          {/* Header bar */}
          <div className="flex items-center gap-2 px-5 py-4 border-b border-border">
            <Shield className="h-4 w-4 text-primary" />
            <span className="text-sm font-semibold text-foreground font-sans">Cybergbeja</span>
          </div>

          {/* Green verification area */}
          <div className="bg-safe/10 px-6 py-8 text-center">
            <div className="mx-auto mb-4 flex h-14 w-14 items-center justify-center rounded-full bg-safe/20 border-2 border-safe/40">
              <ShieldCheck className="h-7 w-7 text-safe" />
            </div>
            <h2 className="text-2xl font-bold text-foreground font-sans">Link Verified</h2>
            <p className="mt-1 text-sm text-muted-foreground">
              Secure connection Established
            </p>
          </div>

          {/* Publisher badge */}
          <div className="flex items-center justify-center px-6 py-4">
            <div className="flex items-center gap-3 rounded-xl bg-background/60 border border-border px-4 py-3">
              <div className="flex h-10 w-10 items-center justify-center rounded-full bg-[#7B5EA7] text-white font-bold font-sans">
                P
              </div>
              <div className="flex items-center gap-2">
                <ShieldCheck className="h-4 w-4 text-safe" />
                <span className="text-sm font-medium text-foreground font-sans">
                  Publisher{"'"}s Identity Confirmed
                </span>
              </div>
            </div>
          </div>

          {/* Destination URL */}
          <div className="px-6 pb-2">
            <div className="flex items-center gap-2.5 rounded-lg bg-background/60 border border-border px-4 py-3">
              <Link2 className="h-4 w-4 shrink-0 text-muted-foreground" />
              <div>
                <p className="text-[10px] text-muted-foreground uppercase tracking-wider font-mono">Destination URL</p>
                <p className="text-sm text-foreground font-mono">{MOCK_URL}</p>
              </div>
            </div>
          </div>

          {/* Security tip */}
          <div className="px-6 py-4">
            <div className="flex items-start gap-2.5 rounded-lg bg-primary/8 border border-primary/15 px-4 py-3">
              <Info className="h-4 w-4 shrink-0 text-primary mt-0.5" />
              <div>
                <p className="text-xs font-semibold text-foreground font-sans">Security Tip</p>
                <p className="mt-0.5 text-[11px] text-muted-foreground leading-relaxed">
                  This link was generated by a verified publisher. It uses end-to-end encryption and has been checked against our global database.
                </p>
              </div>
            </div>
          </div>

          {/* Actions */}
          <div className="flex flex-col gap-3 px-6 pb-6">
            <Button
              className="w-full bg-safe text-safe-foreground hover:bg-safe/85 font-sans font-semibold py-5"
              onClick={onClose}
            >
              Proceed to Link
              <ArrowRight className="ml-2 h-4 w-4" />
            </Button>
            <Button
              variant="outline"
              className="w-full border-border text-foreground hover:bg-background/50 font-sans py-5"
              onClick={onClose}
            >
              Cancel and Go Back
            </Button>
          </div>
        </div>
      </div>
    </div>
  )
}

// ===== SUSPICIOUS =====
export function SuspiciousResult({ onClose }: ResultProps) {
  return (
    <div className="flex min-h-screen items-center justify-center px-4 py-8">
      <div className="w-full max-w-md">
        <div className="overflow-hidden rounded-2xl border border-border bg-card">
          {/* Header */}
          <div className="flex items-center gap-3 px-5 py-4 border-b border-border">
            <button onClick={onClose} className="text-foreground hover:text-primary transition-colors">
              <ArrowLeft className="h-5 w-5" />
            </button>
            <span className="text-sm font-bold text-foreground font-sans uppercase tracking-wider">
              Security Status
            </span>
          </div>

          {/* Warning area */}
          <div className="px-6 pt-8 pb-6 text-center">
            <div className="mx-auto mb-4 flex h-14 w-14 items-center justify-center rounded-full bg-warning/20 border-2 border-warning/40">
              <AlertTriangle className="h-7 w-7 text-warning" />
            </div>
            <h2 className="text-xl font-bold text-warning font-sans uppercase tracking-wide">
              Suspicious Link Detected!
            </h2>
          </div>

          {/* Description */}
          <div className="px-6 pb-5">
            <p className="text-sm text-muted-foreground leading-relaxed text-center">
              This URL leads to an untrusted source. Our system cannot confirm its safety. Please proceed with extreme caution as this could be a phishing attempt to contain harmful content.
            </p>
          </div>

          {/* URL */}
          <div className="px-6 pb-4">
            <div className="flex items-center gap-2.5 rounded-lg bg-background/60 border border-border px-4 py-3">
              <Link2 className="h-4 w-4 shrink-0 text-muted-foreground" />
              <div>
                <p className="text-[10px] text-muted-foreground uppercase tracking-wider font-mono">Destination URL</p>
                <p className="text-sm text-foreground font-mono">{MOCK_URL}</p>
              </div>
            </div>
          </div>

          {/* Actions */}
          <div className="flex flex-col gap-3 px-6 pb-6">
            <Button
              className="w-full bg-card text-foreground border border-border hover:bg-background/50 font-sans font-semibold py-5"
              onClick={onClose}
            >
              <Shield className="mr-2 h-4 w-4" />
              Back to Safety
            </Button>
            <Button
              variant="outline"
              className="w-full border-warning/30 text-warning hover:bg-warning/10 font-sans text-xs py-5"
              onClick={onClose}
            >
              I understand the risk, let{"'"}s me through
            </Button>
          </div>
        </div>
      </div>
    </div>
  )
}

// ===== MALICIOUS =====
export function MaliciousResult({ onClose }: ResultProps) {
  return (
    <div className="flex min-h-screen items-center justify-center px-4 py-8">
      <div className="w-full max-w-md">
        <div className="overflow-hidden rounded-2xl border border-border bg-card">
          {/* Header */}
          <div className="flex items-center gap-3 px-5 py-4 border-b border-border">
            <button onClick={onClose} className="text-foreground hover:text-primary transition-colors">
              <ArrowLeft className="h-5 w-5" />
            </button>
            <span className="text-sm font-bold text-foreground font-sans uppercase tracking-wider">
              Security Status
            </span>
          </div>

          {/* Danger area */}
          <div className="px-6 pt-8 pb-6 text-center">
            <div className="mx-auto mb-4 flex h-14 w-14 items-center justify-center rounded-full bg-destructive/20 border-2 border-destructive/40">
              <ShieldX className="h-7 w-7 text-destructive" />
            </div>
            <h2 className="text-xl font-bold text-destructive font-sans uppercase tracking-wide">
              Malicious Link Detected!
            </h2>
          </div>

          {/* Description */}
          <div className="px-6 pb-5">
            <p className="text-sm text-muted-foreground leading-relaxed text-center">
              Our Security engine has identified this URL as a confirmed threat. Accessing this site may result in{" "}
              <span className="text-destructive font-semibold">identity theft, phishing, or malware installation</span>.
            </p>
          </div>

          {/* Security Report */}
          <div className="mx-6 mb-4 rounded-xl border border-destructive/20 bg-destructive/5 p-4">
            <h3 className="text-xs font-bold text-foreground font-sans uppercase tracking-wider mb-3">
              Security Report
            </h3>
            <div className="flex flex-col gap-3">
              <div>
                <p className="text-[10px] text-muted-foreground uppercase tracking-wider mb-1">Threat Level</p>
                <div className="flex items-center gap-1">
                  {[1, 2, 3, 4, 5].map((i) => (
                    <div
                      key={i}
                      className="h-2 flex-1 rounded-full bg-destructive"
                    />
                  ))}
                </div>
              </div>
              <div>
                <p className="text-[10px] text-muted-foreground uppercase tracking-wider mb-0.5">Threat Type</p>
                <p className="text-sm font-semibold text-foreground font-sans">
                  Phishing/Credential Harvest
                </p>
              </div>
            </div>
          </div>

          {/* URL */}
          <div className="px-6 pb-4">
            <div className="flex items-center gap-2.5 rounded-lg bg-background/60 border border-border px-4 py-3">
              <Link2 className="h-4 w-4 shrink-0 text-muted-foreground" />
              <div>
                <p className="text-[10px] text-muted-foreground uppercase tracking-wider font-mono">Destination URL</p>
                <p className="text-sm text-foreground font-mono">{MOCK_URL}</p>
              </div>
            </div>
          </div>

          {/* Action */}
          <div className="px-6 pb-6">
            <Button
              className="w-full bg-destructive text-destructive-foreground hover:bg-destructive/85 font-sans font-semibold py-5"
              onClick={onClose}
            >
              <ShieldAlert className="mr-2 h-4 w-4" />
              Back to Safety
            </Button>
          </div>
        </div>
      </div>
    </div>
  )
}

// ===== UNSAFE =====
export function UnsafeResult({ onClose }: ResultProps) {
  return (
    <div className="flex min-h-screen items-center justify-center px-4 py-8">
      <div className="w-full max-w-md">
        <div className="overflow-hidden rounded-2xl border border-border bg-card">
          {/* Header */}
          <div className="flex items-center gap-3 px-5 py-4 border-b border-border">
            <button onClick={onClose} className="text-foreground hover:text-primary transition-colors">
              <ArrowLeft className="h-5 w-5" />
            </button>
            <span className="text-sm font-bold text-primary font-sans uppercase tracking-wider">
              Security Status
            </span>
          </div>

          {/* Danger area */}
          <div className="px-6 pt-8 pb-6 text-center">
            <div className="mx-auto mb-4 flex h-14 w-14 items-center justify-center rounded-full bg-destructive/20 border-2 border-destructive/40">
              <ShieldAlert className="h-7 w-7 text-destructive" />
            </div>
            <h2 className="text-xl font-bold text-destructive font-sans uppercase tracking-wide">
              Unsafe Link Detected!
            </h2>
          </div>

          {/* URL */}
          <div className="px-6 pb-4">
            <div className="flex items-center gap-2.5 rounded-lg bg-background/60 border border-border px-4 py-3">
              <Link2 className="h-4 w-4 shrink-0 text-muted-foreground" />
              <div>
                <p className="text-[10px] text-muted-foreground uppercase tracking-wider font-mono">Destination URL</p>
                <p className="text-sm text-foreground font-mono">{MOCK_URL}</p>
              </div>
            </div>
          </div>

          {/* Description */}
          <div className="px-6 pb-5">
            <p className="text-sm text-muted-foreground leading-relaxed text-center">
              This URL has been flagged as a potential phishing threat or contains malicious software designed to steal your information.
            </p>
          </div>

          {/* Actions */}
          <div className="flex flex-col gap-3 px-6 pb-6">
            <Button
              className="w-full bg-primary text-primary-foreground hover:bg-primary/85 font-sans font-semibold py-5"
              onClick={onClose}
            >
              <Shield className="mr-2 h-4 w-4" />
              Back to Safety
            </Button>
            <Button
              variant="outline"
              className="w-full border-destructive/30 text-destructive hover:bg-destructive/10 font-sans text-xs py-5"
              onClick={onClose}
            >
              I understand the risk, let{"'"}s me through
            </Button>
          </div>
        </div>
      </div>
    </div>
  )
}
